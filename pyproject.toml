[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mcp-rag-server"
version = "2.2.1"
description = "Serveur MCP avec RAG complet sur codebase Python/C++ (chunking syntaxique, ChromaDB, mistral-embed) et du HTML"
requires-python = ">=3.11"

dependencies = [
    # MCP
    "mcp",

    # LangChain + Mistral
    "langchain-openai",
    
    # ChromaDB (base vectorielle avec persistance SQLite)
    "chromadb",

    # Chunking C++
    "tree-sitter",
    "tree-sitter-cpp",
    "pymupdf",

    # Utilitaires
    "python-dotenv",

    "pydantic",
    "openai",
    "pyyaml",

    "beautifulsoup4",
    "requests",
    "langchain-community",
    "requests-negotiate-sspi"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
    "ruff",
    "jupyter",
]

# Indique à setuptools que le projet est un répertoire de scripts plats
# (pas de package Python structuré avec __init__.py)
[tool.setuptools]
py-modules = [
    "chunker_code",
    "chunker_cpp",
    "chunker_md",
    "chunker_pdf",
    "chunker_proto",
    "chunker_python",
    "chunker_web",
    "chunker",
    "embedder",
    "indexer",
    "mcp_rag_server",
    "mcp_rag_client_llm",
    "prompts",
    "retriever",
    "store",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
